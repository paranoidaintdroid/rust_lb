[package]
name = "rust_lb"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1

[dependencies]
thiserror = "1"
serde = {version = "1", features = ["derive"]}
toml = "0.8"
tokio = {version = "1.50.0", features = ["full"]}
